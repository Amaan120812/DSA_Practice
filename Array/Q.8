def move_negatives(arr):
    j = 0   

    for i in range(len(arr)):
        if arr[i] < 0:
            arr[i], arr[j] = arr[j], arr[i]
            j += 1

    return arr


arr = [-1, 3, -2, 4, 5, -6]
print(move_negatives(arr))
